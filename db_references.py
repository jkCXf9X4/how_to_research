

            # DOI, title, abstract, URL, "is-referenced-by-count" 
            # query = f"""SELECT * FROM db WHERE {keyword_query}"""
            # c.execute(query)


            # # debug
            # query = f"""SELECT * FROM res"""
            # print(f"{query=}")
            # res = c.execute(query).fetchall()
            # print(len(res))
            # print(res)



            # descibe schema
            # query = f"DESCRIBE db"
            # print(f"{query=}")
            # res = c.execute(query).fetchall()
            # print_seq(res)